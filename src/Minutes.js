function Minutes () {}

Minutes.prototype.convert = function (rawMinutes) {
    var convertedMinutes = {
            prefix : '',
            suffix : ''
        };
    
    return convertedMinutes;
};